[project]
name = "protein-engineering-agent"
version = "0.1.0"
description = "Protein engineering agent with SageMaker async inference and AgentCore Gateway"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.40.12",
    "matplotlib>=3.10.5",
    "mcp>=1.13.0",
    "pandas>=2.3.2",
    "strands-agents>=1.5.0",
]

[dependency-groups]
dev = [
    "boto3>=1.40.12",
    "moto[cloudwatch,s3,sagemaker]==4.2.14",
    "pytest==6.2.5",
    "pytest-cov==4.0.0",
    "pytest-html==3.1.1",
    "pytest-mock>=3.14.1",
    "pytest-timeout==2.1.0",
    "pytest-xdist==3.3.1",
]

[tool.uv.workspace]
members = [
    "agent",
]
